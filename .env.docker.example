# Docker 环境变量配置示例
# 复制此文件为 .env.docker 并修改配置
# 注意：Docker --env-file 不支持带引号的值

# Database（容器内访问宿主机数据库时，使用实际 IP 或 host.docker.internal）
DATABASE_URL=postgresql://user:password@host.docker.internal:5432/database?schema=public

# Redis
REDIS_URL=redis://:password@host.docker.internal:6379/0

# JWT
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=production

# Claude API
CLAUDE_API_KEY=your-claude-api-key
CLAUDE_API_BASE_URL=https://api.anthropic.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Admin
ADMIN_PASSWORD=change-this-password

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://your-domain:3000/api/v1/auth/github/callback
FRONTEND_URL=http://your-domain:3000

# Third Party Proxy (optional)
THIRD_PARTY_PROXY_ENABLED=false
THIRD_PARTY_PROXY_URL=http://proxy-host:port
